class ImageManager {
  static const String logo = 'assets/images/logo.png';
  static const String logoBG = 'assets/images/background.png';


    // !---------------------Recruiter---------------------

  static const String search = 'assets/images/find-person.png';
  static const String upload = 'assets/images/upload.png';

  // define more images if need (only the images)
}

class SvgManager {
  static const String logo = 'assets/svg/logo.svg';

// define more svg if need (only the svg)
}

class AnimationManager {
  static const String dummy = 'assets/animations/dummy.json';

// define more animation if need (only the animation .json or something else)
}

